@model Webshop.UserProfile

@{
  
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" type="text/css" href="~/Scripts/css/StyleSheet1.css">
    <title>Index</title>
</head>

<body>
    <header id="indkøbskurv">
        @if (Model.LoginUser == null)
        {
            @Html.ActionLink("Login", "Index", "Login")
        }
        else
        {
            @Html.ActionLink("Bruger Profile", "userProfile", new { id = Model.id })<br />
            @Html.ActionLink("Log ud", "index", new { id = -1 })
        }

        <div>
            @Html.ActionLink("Indkøbskurv", "Index")
        </div>
    </header>
    <h1>OSTEPAKKEN</h1>
    <div>
        <nav>
            <table id="menutop">
                <tr>
                    <td>@Html.ActionLink("Home", "Index")</td>
                    <td>@Html.ActionLink("Sidste nyt", "Index", new { id = Model.id})</td>
                    <td>@Html.ActionLink("Månedens tilbud", "Index", new { id = Model.id })</td>
                    <td>@Html.ActionLink("Handelsbetingelser", "Index", new { id = Model.id })</td>
                    <td>@Html.ActionLink("Kundeservice", "Index", new { id = Model.id })</td>
                </tr>

            </table>
            <ul id="menu">
                <li>@Html.ActionLink("Discount pris", "DiscountCheese", new { id = Model.id })</li>
                <li>@Html.ActionLink("Hverdagsoste", "Index", new { id = Model.id })</li>
                <li>@Html.ActionLink("Luksusoste", "Index", new { id = Model.id })</li>
                <li>@Html.ActionLink("Eksklusive oste", "Index", new { id = Model.id })</li>
                <li>@Html.ActionLink("Osteborde", "Index", new { id = Model.id })</li>
            </ul>
        </nav>
    </div>

    <section>
        
    </section>
</body>
</html>
